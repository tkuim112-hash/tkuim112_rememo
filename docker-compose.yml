name: tku-smart-care  # 正名為智慧照護專案，展現系統完整性

services:
  # M7: 系統編排器 (中央狀態機)
  app:
    build: .
    container_name: tku-care-app-1
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
      - ./media:/media
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    depends_on:
      - ollama
      #- tts
      - db
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:${POSTGRES_PORT}/${POSTGRES_DB}
    env_file:
      - .env
    restart: unless-stopped

  # M2: 本地大腦 (Llama-3)
  ollama:
    image: ollama/ollama
    container_name: tku-care-ollama-1
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - LD_LIBRARY_PATH=/usr/lib/wsl/lib  # 強制指向掛載進來的驅動路徑
    ports:
      - "11434:11434"
    volumes:
      - ./ollama_data:/root/.ollama
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  rag-service:
    build:
      context: ./Rag
      dockerfile: Dockerfile
    container_name: tku-care-rag-service-1
    ports:
      - "8002:8000"
    volumes:
      - ./Rag:/app                                    # 程式碼熱更新，改完 restart 即生效
      - ./models/ckip_cache:/root/.cache/huggingface  # CKIP 模型只下載一次
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    depends_on:
      - qdrant
      - ollama
    environment:
      - QDRANT_URL=http://qdrant:6333
      - QDRANT_API_KEY=${QDRANT_API_KEY}
      - OLLAMA_HOST=http://ollama:11434
    restart: always

    # 向量資料庫
  qdrant:
    image: qdrant/qdrant
    container_name: tku-care-qdrant-1
    environment:
      - QDRANT__SERVICE__API_KEY=${QDRANT_API_KEY}
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./qdrant_storage:/qdrant/storage
    restart: always

  # M3: 本地語音合成 (BreezyVoice, RTX 50 系列/Blackwell 專用建置)
  tts:
    build:
      context: ./bluemagpie
      dockerfile: Dockerfile
    container_name: tku-care-tts-1
    ports:
      - "9234:8080"
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - LD_LIBRARY_PATH=/usr/lib/wsl/lib
      - MODEL_ID=OpenFormosa/BlueMagpie-TTS
      - MODEL_LOCAL_DIR=/workspace/models/BlueMagpie-TTS
      # 要用你自己的聲音：
      # - SPEAKER_PROMPT_AUDIO_PATH=/workspace/references/audrey.wav
    volumes:
      - ./models/bluemagpie:/workspace/models
      - ./bluemagpie/references:/workspace/references
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: unless-stopped

  # 前端 (Next.js)
  frontend:
    build:
      context: ./therapist-dashboard
      dockerfile: Dockerfile
    container_name: tku-care-frontend-1
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=${FRONTEND_DATABASE_URL}
      - SESSION_SECRET=${SESSION_SECRET}
      - NODE_ENV=production
    env_file:
      - .env
    depends_on:
      - db
    restart: unless-stopped

  # M6: 結構化與向量數據庫 (PostgreSQL)
  db:
    image: postgres:15
    container_name: tku-care-db-1
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    ports:
      - "5432:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data
    restart: always

  # 自動備份 Postgres：每天 pg_dump 一次，壓縮後依天/週/月輪替保留，
  # 存到跟 postgres_data 分開的獨立資料夾（同一顆硬碟壞掉還是會兩邊一起沒，
  # 正式上線後應該再把 ./postgres_backups 同步一份到異地/雲端儲存）。
  db-backup:
    image: prodrigestivill/postgres-backup-local:15
    container_name: tku-care-db-backup-1
    restart: always
    volumes:
      - ./postgres_backups:/backups
    depends_on:
      - db
    environment:
      - POSTGRES_HOST=db
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - SCHEDULE=@daily
      - BACKUP_KEEP_DAYS=7
      - BACKUP_KEEP_WEEKS=4
      - BACKUP_KEEP_MONTHS=6

  # pgAdmin 資料庫管理介面
  pgadmin:
    image: dpage/pgadmin4
    container_name: tku-care-pgadmin-1
    environment:
      - PGADMIN_DEFAULT_EMAIL=${PGADMIN_DEFAULT_EMAIL}
      - PGADMIN_DEFAULT_PASSWORD=${PGADMIN_DEFAULT_PASSWORD}
    ports:
      - "5050:80"
    depends_on:
      - db
    restart: always

  # Redis 快取
  redis:
    image: redis:alpine
    container_name: tku-care-redis-1
    command: redis-server --requirepass ${REDIS_PASSWORD}
    ports:
      - "6379:6379"
    restart: always

  # Redis Insight 管理介面
  redisinsight:
    image: redis/redisinsight:latest
    container_name: tku-care-redisinsight-1
    ports:
      - "5540:5540"
    environment:
      - RI_REDIS_URI=redis://:${REDIS_PASSWORD}@redis:6379
    depends_on:
      - redis
    restart: always
  
  # M1: 感知數據處理服務 (OpenAI Whisper STT)
  kinect-svc:
    image: fedirz/faster-whisper-server:latest-cuda
    container_name: tku-care-kinect-1
    ports:
      - "8001:8000"  # 規格書定義 Port 8001 [cite: 20, 24]
    environment:
      - WHISPER_MODEL=base  # 第一次跑建議先用 base，速度最快
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
    restart: always